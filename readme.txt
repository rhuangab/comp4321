To execute the project, please follow the following steps.

1. Import the whole project into eclipse, notice that you may need to import the library to java project.

2. Run the Spider.java which contains the main method. After that the database will be generated.

3. Run the TestProgram.java, after that, a spider_result.txt will be generated which contains all the information.